(function (){
    'use strict';
     ACMESTORE.articles.display = function () {
       /*  var app = new Vue({
            el:'#article',
            data: {
                articles: [],
                loading: false,
                next: 8,
                targetElement: $('.article'),
                loadMoreEndpoint: '/articles/category/load-more'
            },
    
            methods: {
                getArticleCategory: function () {
                    var token = this.targetElement.data('token');
                    var urlParams = this.targetElement.data('urlparams');
                    this.loading = true;
                    var postData = { next: this.next, token: token, count: this.count };

                    if(typeof urlParams !== 'undefined' && urlParams){
                        postData.title = urlParams.title;
                       
                    }
                },
                stringLimit: function (string, value){
                    if(string.length > value){
                        return string.substring(0, value) + '...';
                    }else{
                        return string;
                    }
                }
            },
    
            created: function () {
                this.getArticleCategory();
            }
         }); */
     }
    })();